resp = self.visit(tree.children)
    first = resp[0]
    flag = True #Para saber se há algum operador a escrever no meio
    if len(tree.children) == 3:
      second = resp[2][0]
      l = {first, second}
    
else:
      l = {first}
      flag = False
    for var in l:
      if var.type == "WORD":
        if var not in self.varsDecl:
          self.varsNDecl[var] = {}
          self.html = self.html + "<div class='error'> "+ var + "<span class='errortext'>Variável não declarada</span></div>"
        else:
          self.varsDecl[var]["utilizada"] += 1 
          if self.varsDecl[var]["inic"] == 0:
            self.html = self.html + "<div class='notinic'> "+ var + "<span class='notinic'>Variável não inicializada</span></div>"
          else:
            self.html = self.html + var 
      else:
        self.html = self.html + " " + var 
      if (flag):
          self.html = self.html + " " + resp[1][0] + " "
          flag = False
        

  